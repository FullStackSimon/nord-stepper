import { ReactiveController, ReactiveControllerHost } from "lit";
export declare class ResizeController implements ReactiveController {
    private target;
    private observer?;
    private hadFirstUpdate;
    private borderBoxSize?;
    constructor(host: ReactiveControllerHost, target: () => HTMLElement);
    get inlineSize(): number | undefined;
    get blockSize(): number | undefined;
    hostUpdated(): void;
    hostConnected(): void;
    hostDisconnected(): void;
}
