import { ReactiveController, ReactiveControllerHost } from "lit";
type PortalOptions = {
    outlet: HTMLElement;
    render: () => unknown;
};
export declare class PortalController implements ReactiveController {
    private host;
    private options;
    private renderHook;
    private lightDom;
    constructor(host: ReactiveControllerHost & HTMLElement, options: PortalOptions);
    hostConnected(): void;
    hostUpdated(): void;
    hostDisconnected(): void;
}
export {};
