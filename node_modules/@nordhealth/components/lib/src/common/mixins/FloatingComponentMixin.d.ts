import { LitElement } from "lit";
import { Alignment } from "@floating-ui/dom";
import { LogicalSide } from "../positioning.js";
type Constructor<T = Record<string, unknown>> = new (...args: any[]) => T;
export declare class FloatingMixinInterface {
    open: boolean;
    align: Alignment;
    position: LogicalSide | "auto";
}
export declare function FloatingMixin<T extends Constructor<LitElement>>(superClass: T): Constructor<FloatingMixinInterface> & T;
export {};
