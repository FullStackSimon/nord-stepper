import { LitElement } from "lit";
import type { Ref } from "lit/directives/ref.js";
type Constructor<T = Record<string, unknown>> = new (...args: any[]) => T;
export declare class NotificationMixinInterface {
    protected dismissed: boolean;
    protected notificationRef: Ref<HTMLElement>;
    dismiss(): Promise<void>;
}
export declare function NotificationMixin<T extends Constructor<LitElement>>(superClass: T): Constructor<NotificationMixinInterface> & T;
export {};
